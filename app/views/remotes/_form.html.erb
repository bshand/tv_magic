<%= form_with(model: remote, local: true) do |form| %>
  <% if remote.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(remote.errors.count, "error") %> prohibited this remote from being saved:</h2>

      <ul>
        <% remote.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description, size: 80 %>
  </div>

  <div class="field">
    <%= collection_check_boxes(:remote, :tv_ids, @tvs, :id, :make_and_model) %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
